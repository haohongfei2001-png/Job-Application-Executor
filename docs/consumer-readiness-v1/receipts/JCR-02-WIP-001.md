# JCR-02 execution receipt — in progress

Status: IMPLEMENTING. This is a checkpoint, not round certification.

## READ_FIRST and writer

- Remote main base: `f09f419af4eb6d9cda7b4a5efd0641b8eaf790c7`, verified after PR #11 merged and both main CI jobs passed in [run 35897981884](https://github.com/haohongfei2001-png/Job-Application-Executor/actions/runs/35897981884).
- Canonical main readback: JCR-01 `ADVANCE_ALLOWED_WITH_DEFERRED`, JCR-02 `READY` before this branch, DFG-001 `MITIGATED_FOR_ENGINEERING`.
- Open PRs / active writer at start: none. Sole JCR-02 writer branch: `feat/jcr02-owned-browser-recovery`.
- Contract: `05_ROUNDS.md` JCR-02, `02_ARCHITECTURE.md` browser ownership and recovery ADRs, `04_ACCEPTANCE_MATRIX.md` G-01–G-13, `06_EXECUTION_PROTOCOL.md`.

## Current implementation and evidence

- The production generic adapter now selects only a unique popup opened by the current task page. An unrelated newer tab is ignored; multiple successors, a closed page without an owned successor, or a cross-origin successor cause a typed observation error.
- Worker records `browser_ownership_unknown` and stops automatic replay. Local resume is refused even after a subsequent pause until a read-only reconciliation mechanism exists; this dependent path is DFG-002.
- A live worker also binds its lease to a fingerprint of the owned CDP listener PID, process start time and dedicated profile inode. A changed process epoch fences the next browser mutation as `browser_ownership_unknown`; synthetic process stubs verify this without opening or touching user Chrome.
- Unit tests cover unrelated tabs, unique popup, ambiguous popup, closed page, cross-origin successor and no blind replay. A real isolated headless Chromium test opened an owned popup from a loopback HTTP fixture while an unrelated tab was newer.
- The first JCR-02 local full isolated regression after page-lineage replacement passed 279 tests in 45.66 seconds. With the process-epoch fence and its two new regressions, the full isolated suite passed 281 tests in 44.62 seconds. Exact-head CI remains pending.
- A durable `run_attempts` journal now persists `ATTEMPTED` before the runner can write externally. A normal return is recorded as `RETURNED_UNVERIFIED`; a thrown ownership/unknown error or a replacement worker seeing an expired lease with an unfinished attempt becomes `UNKNOWN_OUTCOME` and blocks automatic replay/resume. The new table is additive and keeps task IDs and existing command receipts. Crash recovery and pause-after-unknown regressions pass. The full isolated suite with this change passed 282 tests in 44.69 seconds.
- The consumer launcher now starts and checks the local supervisor before attempting dedicated browser repair. Missing model/profile/Chrome no longer prevents the authenticated local task panel from opening. The panel fetches a separate read-only readiness report and displays a specific remediation message; endpoint liveness is not labeled live E2E readiness.
- If the supervisor cannot start, a separate loopback-only bootstrap process serves a minimal recovery page with a safe failure reason and local code version. Its random URL capability and same-origin POST requirement protect the retry operation. Retrying starts only the owned local supervisor and redirects through a fresh single-use UI ticket. A real isolated process test verifies service failure page, unauthorized rejection, retry, health and redirect. No visible external browser or user Chrome profile was used in the test.
- A further browser review found that live connection previously closed duplicate exact-target and blank tabs and selected the last exact-target tab. Live connection now leaves existing tabs untouched, creates a fresh tab for a first run, and blocks if an unbound exact-target tab is present. A second live run after an unverified prior attempt stops before browser access until durable task/tab reconciliation is implemented. Synthetic fake-CDP regressions cover both fences.
- Diagnostics now report `worktree_clean: null` when Git status cannot be read, instead of claiming a clean checkout without evidence. A failure-path regression covers this.
- Full isolated regression after the live tab-selection fence and diagnostics correction: 287 passed in 45.68 seconds.
- Additive `browser_bindings` metadata now records task-to-CDP-target, owned process and current document epoch under the worker lease. The adapter writes a first binding before navigation, records the new document after navigation, and only transfers a binding to a popup whose opener is the bound page. Each live mutation checks the current page and document immediately before acting. A worker handoff test proves the binding survives a new queue instance. A real headless Chromium test confirms CDP target IDs differ between parent and popup while remaining stable for one page. Observation failure raises a typed error rather than appearing as an empty form; uncertain field/OTP primitive failures block as unknown instead of silently returning false.
- The binding and observation targeted regression passed 62 tests; a subsequent worker-to-queue binding regression brought the targeted browser runtime module to 17 passing tests. Exact-head CI for these changes is pending.
- Full isolated regression after the complete binding checkpoint: 293 passed in 45.51 seconds.
- A read-only observe path now checks only an already-bound process/tab/document. It never starts a browser, navigates, fills or releases the task. The local authenticated UI and CLI can request it for a blocked unknown outcome; the UI hides the unusable Resume control and says explicitly that finding the old page does not prove draft identity or write success. The result always carries `replay_allowed=false`, `draft_identity_verified=false` and `write_outcome_verified=false`. Targeted observer/UI/session tests: 51 passed. This is a diagnostic foundation, not recovery certification.
- Full isolated regression after the read-only observation path: 296 passed in 45.44 seconds. Previous binding head `731d297` passed both exact-head CI jobs in run 35903977833; observation head CI is pending.
- Full local isolated regression after bootstrap and read-model changes: 284 passed in 46.39 seconds. Targeted recovery/readiness suite after final bootstrap reason/version refinement: 34 passed in 1.96 seconds. Full-suite rerun on final PR head remains pending.
- PR [#12](https://github.com/haohongfei2001-png/Job-Application-Executor/pull/12) is the sole draft writer. Heads `b53a305` and `34639e9` each passed foundation and full test CI; the bootstrap follow-up still needs a new exact-head run.
- Real user Chrome profile, real account/job, SMS, upload and final submit: not accessed or modified.

## Outstanding JCR-02 work

Implement read-only draft identity and UNKNOWN_OUTCOME reconciliation, per-action observed receipts and human handoff coordination. Current second-run live block is a deliberate temporary safety fence; the new browser binding alone is not a certified recovery flow. Run browser kill/restart/draft oracle and human handoff tests, exact-head CI, review, merge and main readback. G-01–G-13 remain NOT_RUN / UNVERIFIED until their required evidence exists. DFG-001 remains JCR-04 fact-recovery work.
