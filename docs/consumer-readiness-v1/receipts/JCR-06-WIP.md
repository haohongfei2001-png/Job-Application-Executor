# JCR-06 engineering checkpoint — IN_PROGRESS

Canonical package: `JAE-CONSUMER-READINESS-v1`. Sole writer: draft PR #16, `feat/jcr06-structured-forms`. This is a work receipt, not JCR-06 closure or an acceptance PASS.

## Verified synthetic subevidence

- `FormObservation` and `FillPlan` are separate. The copy-safe structural summary omits field values and labels. Hidden required controls, duplicate selectors, unsupported components and site validation indicators are observed. A parent write triggers bounded fresh observation; new required fields are either resolved from the canonical profile or block.
- Duplicate generic selectors do not write the first repeated row. Read-only observation counts repeated rows with hashed site markers and flags reused/ambiguous row identities without copying raw tokens into structural receipts. Same-title canonical project records require distinct `record_id` bindings. An observed but unparsed resume research/project section blocks project coverage. English research/project headings are classified.
- Even when two generic DOM rows have unique site markers and unique field selectors, the executor now blocks before filling them: those markers alone cannot bind the rows to distinct canonical education/project records. Only a site driver with independent row inventory and reconciliation can opt in. A headless two-row counterexample verifies the block occurs before any field plan is created.
- A supported ARIA combobox needs one controlled listbox, exact visible choice and post-selection readback. Missing virtualized choices remain unsupported. Date controls reject a month-only fact. A scoped salary quote requires a user-confirmed exact target, currency, annual/monthly period, tax basis, explicit unit and site step/bounds.
- Native select writes now require exactly one matching option before any selection. Duplicate exact labels with different option IDs, empty candidates and multi-select controls are unsupported by the generic driver; a successful single choice is read back by selected index. The wrong-option oracle still fails. The affected D-01/JCR-01 contract and historical safety set passed **109 isolated/headless tests** after this increment.
- Explicit site `data-depends-on` metadata now orders a parent control before its dependent field even when DOM order is reversed; the child is located again after redraw, and the option set participates in the observation digest. Missing, cyclic, unresolved or failed parent dependencies block instead of filling a stale child. This is isolated synthetic support for an explicit dependency contract, not general proof for unannotated real ATS controls.
- A pinned React 18 controlled-component fixture now runs in isolated headless Chromium against a loopback draft API. A forced framework rerender retains the expected input and framework state; the independent server oracle records the same value. The executor reaches synthetic READY only with that server readback. If the API discards the write, it blocks with no submit call. The test bundle, source, build record and MIT notices are checked in, with no runtime CDN request.
- A pinned Vue 3.5.18 controlled-component fixture now repeats the same isolated browser → actual framework state → loopback server draft check. Forced rerender preserves the canonical value; a wrong draft canary fails and a dropped server write blocks READY, with submit count zero. The browser bundle, source, exact npm dependency lock, build digest and MIT notices are checked in; no browser runtime CDN request is used. React and Vue synthetic coverage does not imply real ATS component support.
- A certified site driver can compose the new `RowReconciler` and scoped SQLite `RowActionJournal`. It requires a complete independent row inventory, stable canonical record bindings and monotonic draft revisions. Pre-action intent is committed before add/delete/reorder; journal storage contains digests rather than applicant values or site row IDs. A real child process exits immediately after a deliberately non-idempotent fake server add; a new reconciler settles the pending action from readback and does not add another row. An absent pending add, unmanaged extra row, changed target/draft scope, stale concurrent inventory or regressed draft revision blocks. Explicit managed deletion and reordering are tested against the fake server. No generic live row write is enabled by this component.
- The row reconciler also exposes an explicit read-only pending-effect recovery entrypoint. A revoked task lease immediately after durable intent creation leaves a pending action but performs zero site writes; read-only observation then refuses to replay the absent add. When the prior server effect exists, read-only observation settles it with no second add. Row writes require an explicit task mutation guard; omitting it blocks before journal intent or site action.
- Both read-only row recovery and new row writes now require the independent site inventory to identify the exact target hash and draft identity hash expected by the task. A wrong draft response blocks before journal intent or site effect; the targeted row recovery suite passed eight tests. This is prerequisite binding for a certified adapter and does not enable generic site row writes.
- The application and worker now wire a certified `RowExecutionContract` to a task-scoped row journal and lease guard. The contract must cover every canonical record and every known field in its declared collection with exact source values; unknown, invented or silently omitted records/fields block before writes. Deletion is limited to a matching task-scoped, reasoned canonical project exclusion and a driver-proven managed site row. A fake ATS education collection adds two records once, reruns without a duplicate, then blocks wrong-draft, invented-school, omitted-record and omitted-degree canaries. This is an API-backed synthetic driver, not a generic live ATS capability or complete D-05/D-08 certification.
- Resume and photo slots are distinct. A canonical file hash, declared MIME/extension and site size limit are checked before a site upload driver may act. Generic file inputs remain blocked because browser selection does not prove upload completion or retained draft state.
- A certified upload driver can now report one independent server readback per selected resume/photo slot. The executor checks the exact target hash, canonical bytes hash, distinct retained server asset identities, one shared draft identity and positive draft revision; the final draft readback must identify that same draft at an equal or later revision. Only aggregate slot count/revision/level enter plan metadata. A loopback upload service proves both files reach one draft and zero submit calls; dropping the photo or returning another draft identity blocks. Unverified upload outcomes have a durable non-resumable worker blocker. The generic file driver still does not upload.
- The upload oracle now also drives actual resume/photo browser file controls on a local HTML page. A synthetic site-specific adapter selects the two canonical files in isolated headless Chromium; the page uploads their bytes to the fake service, and the executor compares independent server hashes and draft receipts. A deliberately dropped photo upload blocks even though browser selection completed; submit count stays zero. This is a certified synthetic driver fixture, not authorization to enable generic live uploads.
- Across a supported multi-page execution, the executor re-reads all earlier selected attachments from the independent server draft immediately before READY. A two-page fake ATS that discards the photo during Next blocks at final review, while the retained-photo control reaches synthetic READY; both record zero submit calls.
- A private atomic attachment manifest now records only target/task scope hash, canonical file hashes, server draft identity hash, slot names and revision after proven upload. The worker assigns a task-scoped manifest path. A restarted synthetic executor on the later page loads that manifest and independently re-reads both server attachments without a second upload; a dropped photo blocks. A restarted executor on the original file page blocks before replay, and changed task scope or canonical hash invalidates the manifest. This covers the synthetic cross-process checkpoint, while the worker's broader browser/draft unknown-outcome recovery in DFG-002 remains unresolved.
- A generic DOM value or clicked save button does not certify persistence. The loopback SyntheticATS test compares a server draft and revision against a golden draft, and a wrong-value canary fails. A missing proof creates `draft_persistence_unverified`, which cannot be resumed blindly after restart.
- DOM observation exceptions now have an explicit `form_observation_unavailable` blocker and an `ERROR` observation marker; neither an initial failed read nor a failed redraw after a field write can become READY. A pause/resume regression exposed that pausing could erase existing draft/auth uncertainty blockers. The queue now preserves form, draft, attachment, auth-return and account-identity uncertainty through Pause, and refuses ordinary Resume for each. The affected isolated/headless D-18, application, browser and historical privacy/contract set passed **116 tests**.
- Before a supported driver clicks a multi-page Next control, the executor now requires an independent draft readback and records only its evidence level and revision. The two-page loopback ATS checks the first page's server draft before navigation and the combined draft at final review. Fresh isolated browser contexts re-enter both pages and observe values restored from the server draft. A server that discards the first write blocks before the second page is opened; the synthetic submit counter stays zero. That block maps to the durable `draft_persistence_unverified` state, which refuses blind Resume, with a `before_navigation` phase marker. Generic sites with no independent readback stay blocked at that navigation.
- General standing consent flags no longer authorize unseen statement text or another target. A default checked declaration without exact user-confirmed text and target digests blocks.

Local isolated/headless full suite after the scoped row journal and subprocess crash test: **416 passed in 87.76s**. The subsequent revision-regression safety change passed the focused row recovery suite (**4 tests**). The attachment read-error classification passed its focused regression. Remote main's cadence amendment in PR #17 makes draft CI run foundation/compile/diff while the broad `test` job is intentionally SKIPPED; a skipped job is not recorded as PASS. Exact-head full GitHub CI is due when JCR-06 is stable and PR #16 becomes Ready for round closure. No review thread is open as of this checkpoint.

The upload receipt increment passed **78 targeted form, application, browser-recovery and historical privacy tests** in isolated/headless mode. This is incremental evidence under the current risk-based cadence, not D-class round certification or exact-head full CI.

After the read-only row recovery, explicit lease guard and multi-page attachment retention increments, the affected isolated/headless form, row, application, browser-recovery and historical privacy set passed **87 tests**. This remains targeted evidence.

The certified row path now reobserves every bound collection read-only immediately before READY and binds the final server draft receipt to the same draft identity and at least the latest row revision. A dropped row after navigation and a mismatched draft receipt both block. A combined isolated headless browser fixture uploads both files through actual file controls, adds two canonical education rows through a deliberately non-idempotent API, advances to a review page, verifies one shared independent server draft, and returns to that page using the private attachment manifest and scoped row journal without another upload or row add. Removing a row before return blocks without replay; submit count remains zero. This proves the synthetic composed execution path, not an external ATS driver. The affected form, React/Vue, row, attachment, queue, OTP and historical privacy set passed **109 tests** on this checkpoint; the full suite and exact-head CI remain due at stable round closure.

Row journals are now scoped by canonical collection as well as task, target and draft. A returning execution must reobserve every collection with a prior journal before another row mutation or READY. An earlier journal with no collection identity, an absent site driver or an unreconciled second collection blocks; none of those cases replays a row add. The focused synthetic row/attachment set passed **14 tests** after this change.

A certified site driver may provide distinct education and project row contracts in one task. Their journals stay separate even when both collections share a draft identity, and a second execution reads both without duplicate adds. An unmanaged row matching a canonical ID now blocks before any other row is added. The affected isolated/headless form, row, attachment, React/Vue, queue, OTP and historical privacy set passed **111 tests** after this increment. This is synthetic multi-collection coverage; no external ATS driver or complete D-class gate is claimed.

All offered row contracts now establish one draft identity before the first row mutation; a mismatched second collection blocks with zero adds. Row inventories must provide actual Boolean management/completeness flags, an integer revision, typed row identities and SHA-256 value digests; a string such as `"false"` cannot assert ownership. The focused row fault set passed **12 tests** after this safety tightening.

Unsupported initial form structures now carry fixed, copy-safe capability codes for unbound repeated rows, unsupported components, ambiguous field/row identity and empty final forms. The task stays blocked without asking for a missing applicant fact or exposing site labels in those codes. The affected isolated browser form, row and attachment set passed **50 tests** after this classification and the row-proof tightening.

The JCR-06 row contract now recognizes JCR-04's canonical project shape with a top-level sourced title, and validates every offered collection before the first row mutation. It accepts omission only from the task-scoped `user_explicit_task` exclusion with a nonempty reason. Final review counts a project row only after the independent final row inventory has verified its canonical ID, values and draft; an unapproved title-only plan annotation no longer erases a coverage gap. Malformed or duplicate canonical project records remain REVIEW_REQUIRED. A two-collection synthetic task proves one education row plus one research/project record, a separately approved exclusion, idempotent rerun and zero auto submit. The affected JCR-04/JCR-06 application, review, row and form set passed **106 isolated/headless tests**. This does not certify an external ATS project driver.

For a certified project driver, an existing managed row is deleted only when the same canonical record has a task-scoped `user_explicit_task` exclusion with a nonempty reason; an unapproved deletion blocks before any row action. An isolated two-collection task deletes that one excluded row, adds the included row, and repeats without another delete or add. Separate real child-process crashes after server-side delete and reorder are reconciled from read-only inventory without replay. The focused row/review fault set passed **19 tests**. These are synthetic effect and recovery proofs, not an external-site deletion authorization.

Project row values now include the canonical `project` or `research` category in the independent site digest, so a title match cannot silently place a research record in another kind of experience row. `project_or_research`, `ambiguous` or missing category blocks before any row write; a synthetic ambiguous-category canary leaves both collections unchanged. A real ATS driver must independently expose equivalent section identity or remain unsupported. The focused row/application/review set passed **46 tests** after this gate.

The generic observer now flags form controls inside an open shadow root and visible opaque custom elements as unsupported; a visible iframe remains unsupported as before. Actual isolated browser fixtures confirm a required input exists inside the shadow root and iframe, yet neither can disappear from observation and yield false READY. A site driver is required to support either component. The affected form, React/Vue, attachment, row and historical privacy set passed **62 isolated/headless tests**.

## Still open

D-01–D-22 are not certified as a set. The row reconciler is integrated with the application and a certified synthetic driver; no real ATS row driver or complete worker browser/draft recovery is proven. Virtualized list support, unannotated site dependency drivers, external site upload completion receipts, broader multi-page recovery and per-action fault injections remain. The generic unsupported paths are blocked. DFG-002 browser write reconciliation is engineering debt; it is not a final-live owner item. DFG-007 records real applicant/site draft evidence as FINAL_LIVE while this engineering continues. No owner action or automated final submit is authorized by this checkpoint.

The scoped row journal and private attachment manifest are additive new artifacts; no existing row/attachment store is migrated. `FieldResolution.record_id` is optional for old plans. Rolling back to a writer that ignores pending row/attachment effects or the new draft/browser proof gates must leave affected tasks blocked and read-only; it must not replay add, delete, upload or navigation from an old locator.
