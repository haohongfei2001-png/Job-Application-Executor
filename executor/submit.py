from __future__ import annotations

def submit_current(confirm: str):
    raise RuntimeError(
        "automated final submission is disabled: the final submit control must be clicked by the user"
    )
